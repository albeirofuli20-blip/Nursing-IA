import { useState } from "react";
import { base44 } from "@/api/base44Client";

export default function usePlanGenerator(onSaved) {
  const [loading, setLoading] = useState(false); const [error, setError] = useState("");
  async function generate(patient, notes, file, guides) { setLoading(true); setError(""); try { let file_urls; if (file) { const uploaded = await base44.integrations.Core.UploadFile({ file }); file_urls = [uploaded.file_url]; } const context = guides.slice(0, 5).map(g => `${g.title}: ${g.content}`).join("\n"); const response = await base44.functions.invoke("aiInvoke", { action: "generate_plan", prompt: `Actúa como asistente clínico de enfermería. Propón un Plan de Atención de Enfermería en español basado en las taxonomías NANDA-I, NOC y NIC, usando los datos del paciente. No inventes signos ni antecedentes. El profesional revisará el resultado. Paciente: ${JSON.stringify(patient)}. Notas adicionales: ${notes}. Guías disponibles: ${context}. Devuelve: título del PAE, valoración, diagnóstico médico inferido si hay datos, y para cada diagnóstico NANDA incluye código, definición, factores relacionados, características definitorias, dominio y clase. Para cada resultado NOC incluye código, indicadores y puntuaciones inicial/esperada (1-5). Para cada intervención NIC incluye código, actividades y fundamentación científica con cita de fuente cuando sea posible.`, file_urls, response_json_schema: { type: "object", properties: { title: { type: "string" }, medical_diagnosis: { type: "string" }, assessment: { type: "string" }, diagnoses: { type: "array", items: { type: "object", properties: { nanda: { type: "string" }, code: { type: "string" }, definition: { type: "string" }, related_to: { type: "string" }, evidence: { type: "string" }, domain: { type: "string" }, class: { type: "string" } } } }, outcomes: { type: "array", items: { type: "object", properties: { noc: { type: "string" }, code: { type: "string" }, indicators: { type: "array", items: { type: "string" } }, scale_initial: { type: "string" }, scale_expected: { type: "string" } } } }, interventions: { type: "array", items: { type: "object", properties: { nic: { type: "string" }, code: { type: "string" }, activities: { type: "array", items: { type: "string" } }, rationale: { type: "string" } } } }, execution: { type: "string" }, evaluation: { type: "string" }, diana_score: { type: "string" } }, required: ["title", "assessment", "diagnoses", "outcomes", "interventions"] } }); const result = response.data; await base44.entities.CarePlan.create({ ...result, patient_id: patient.id, patient_name: patient.full_name, status: "borrador", ai_generated: true }); onSaved(); return true; } catch { setError("No se pudo generar el plan. Revisa los datos e inténtalo de nuevo."); return false; } finally { setLoading(false); } }
  return { generate, loading, error };
}